<%= form_for current_user.relationships.build, remote: true do |f| %>
  <div><%= hidden_field_tag :comic_id, @comic.id %></div>
  <div><%= hidden_field_tag :like, Settings.like %></div>
  <li class="li03">
    <span class="btn-like-parent">
      <i class="fa fa-thumbs-o-up"></i>&nbsp;
      <%= f.submit t(".like"), class: "btn-like" %>
    </span>
  </li>
<% end %>
